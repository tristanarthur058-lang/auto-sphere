# En développement (SQLite), aucune variable n'est requise :
# le schéma pointe directement vers prisma/dev.db

# En production, bascule le datasource de prisma/schema.prisma
# sur PostgreSQL puis renseigne :
DATABASE_URL="postgresql://user:password@localhost:5432/autoparts?schema=public"
